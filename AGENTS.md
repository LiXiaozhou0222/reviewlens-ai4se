# 当前阶段工作约束

本仓库选择 AI4SE 期末项目 B 类“非 Harness 应用类项目”，当前处于需求澄清、规约设计与实现计划阶段。

本文件适用于所有主智能体、子智能体、评审智能体和冷启动验证智能体。除非用户明确修改本文件并解除限制，否则不得自行进入实现阶段。

## 一、当前阶段目标

当前阶段必须依次完成：

1. 阅读课程通用要求与 B 类项目要求；
2. 使用 Superpowers 的 `brainstorming` skill 进行需求澄清；
3. 经用户分阶段确认设计；
4. 生成完整的 `SPEC.md`；
5. 使用 Superpowers 的 `writing-plans` skill 生成 `PLAN.md`；
6. 在 `SPEC_PROCESS.md` 中记录真实的规约生成过程；
7. 使用另一种陌生智能体完成冷启动试运行；
8. 根据冷启动结果修订 `SPEC.md` 与 `PLAN.md`；
9. 由用户明确确认是否解除本阶段限制。

## 二、允许的工作

当前阶段允许：

- 阅读以下课程要求及已有项目文档：
  - `docs/requirements/AI4SE_Final_Project_通用要求.md`
  - `docs/requirements/AI4SE_Final_Project_B_应用类项目.md`
  - `README.md`
  - `AGENTS.md`
- 使用 Superpowers 的 `brainstorming` 和 `writing-plans` skills。
- 进行要求审计、需求分析、方案比较和架构讨论。
- 创建或更新：
  - `AGENTS.md`
  - `SPEC.md`
  - `PLAN.md`
  - `SPEC_PROCESS.md`
  - `AGENT_LOG.md`
  - `README.md` 中与项目定位和当前阶段有关的说明
  - `docs/cold-start/` 下的冷启动说明和真实验证记录
- 检查 Git 状态和已有文档差异。
- 为规约和计划工作创建合理的文档提交。

课程原始要求文件只允许读取，不得擅自修改、删减、重命名或覆盖。

## 三、明确禁止的工作

在本文件明确解除限制之前，禁止：

- 创建、修改或删除任何业务实现代码。
- 初始化前端、后端、数据库或其他应用框架。
- 创建 `apps/`、`src/` 等业务实现目录。
- 安装或修改业务依赖。
- 创建可执行的业务脚手架。
- 创建 Dockerfile、Docker Compose、CI 配置、数据库迁移或部署配置。
- 编写单元测试、集成测试或端到端测试。
- 生成或伪造测试输出、测试结果、覆盖率、CI 记录或部署记录。
- 将计划、示例、接口草图或伪代码落地为可执行实现。
- 创建虚假的 commit hash、PR、MR、评审记录或冷启动证据。
- 创建、修改、代写或补全 `REFLECTION.md`。
- 声称功能、测试、CI、分发或部署已经完成。
- 将单轮 LLM 调用扩展为自主循环、工具自主调用或自动执行代码的 Agent 功能。
- 自行宣布已经满足解锁条件。

## 四、规约与计划要求

### `SPEC.md`

必须至少包含：

- 问题陈述与目标用户；
- 至少 5 个符合 INVEST 原则的用户故事；
- 按模块划分的功能规约；
- 输入、行为、输出、边界条件与错误处理；
- 性能、安全、可用性和可观测性要求；
- 凭据威胁模型与对策；
- 系统架构与数据流；
- 数据模型；
- 技术选型与理由；
- 凭据与分发方案；
- 客观验收标准；
- 风险与未决问题；
- B 类非 Agent 边界。

### `PLAN.md`

必须由 `writing-plans` skill 生成，并满足：

- 每个 task 有唯一编号；
- 每个 task 足够小，可由一个新鲜 subagent 独立完成；
- 写明目标、精确文件路径、依赖和实现要点；
- 写明将要先编写的失败测试；
- 写明 RED、GREEN、REFACTOR 的验证步骤；
- 标明 task 间依赖和可并行部分；
- 标明计划使用的 worktree 和分支；
- 不得预填不存在的完成状态或 commit hash。

### `SPEC_PROCESS.md`

必须真实记录：

- brainstorming 的关键问题；
- 至少 3 轮关键迭代；
- 用户采纳、否决或修改 AI 建议的决定及理由；
- 设计签字确认过程；
- 冷启动中暴露的歧义；
- 根据冷启动结果对 SPEC 和 PLAN 所做的修订；
- 修订前后的关键差异。

### `AGENT_LOG.md`

当前阶段也必须记录：

- 时间戳；
- 当前阶段或 task；
- 使用的 Superpowers skill；
- 关键 prompt/context；
- 智能体建议；
- 用户的确认、否决和人工修改；
- 偏离课程流程的情况及理由；
- 学到的教训。

## 五、陌生智能体冷启动要求

冷启动验证必须同时满足：

1. 使用与主开发智能体不同类型的智能体；
2. 开启全新 session；
3. 不导入主智能体的历史会话或 memory；
4. 只向它提供 `SPEC.md` 和 `PLAN.md`；
5. 不提供额外口头解释；
6. 让它从 `PLAN.md` 中选择 1—2 个 task 进行试运行；
7. 要求它遇到不确定之处立即暂停提问，不得猜测；
8. 记录它暂停的位置、错误理解、暴露的规约缺陷和产出差距；
9. 将结果写入 `SPEC_PROCESS.md`；
10. 根据结果实际修订 `SPEC.md` 和 `PLAN.md`。

冷启动期间产生的试运行代码只能用于验证规约清晰度，不得直接视为正式实现成果；是否保留或丢弃由用户在验证完成后决定。

## 六、解锁条件

只有以下条件全部满足，才具备进入实现阶段的资格：

1. `SPEC.md` 已完成并经用户明确确认；
2. `PLAN.md` 已完成并经用户明确确认；
3. `SPEC_PROCESS.md` 已记录至少 3 轮关键迭代；
4. 已由另一种陌生智能体完成 1—2 个 task 的冷启动试运行；
5. 冷启动问题及修订前后差异已记录；
6. `SPEC.md` 和 `PLAN.md` 已根据冷启动反馈完成修订；
7. 用户明确确认解除本文件的阶段限制。

即使上述文件已经存在，智能体也不得自行判定已经解锁。

如用户请求与本文件冲突，必须先指出冲突，并请求用户明确更新本文件。未经用户明确授权，不得执行冲突请求。
